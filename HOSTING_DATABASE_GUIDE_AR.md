# دليل رفع ZON Store وضبط قاعدة البيانات

## قبل البدء

يمكن تشغيل ZON Store على استضافة Node.js تدعم عملية خادم دائمة، وMySQL 8 أو TiDB، ومتغيرات بيئة، وHTTPS. لا يكفي رفع ملفات الواجهة إلى خدمة استضافة ثابتة؛ فالمتجر يستخدم Express وtRPC وShopify Storefront API وقاعدة بيانات لتسجيل المستخدمين وتتبع الطلبات.

> الاستضافة المدمجة بالمشروع تظل الخيار الأسرع لأنها تضبط البنية الأساسية تلقائياً. إذا اخترت استضافة خارجية، اتبع الخطوات التالية بعناية واضبط OAuth وShopify والقاعدة لدى المزود الجديد.

## 1. ارفع المصدر وثبّت الاعتمادات

فك أرشيف المشروع على الاستضافة، ثم من مجلد الجذر الذي يحتوي `package.json` شغّل:

```bash
corepack enable
pnpm install --frozen-lockfile
pnpm check
pnpm test
pnpm build
```

عيّن أمر البناء في منصة الاستضافة إلى `pnpm build` وأمر التشغيل إلى `pnpm start`. يجب ألا تثبت رقم منفذ داخل الكود؛ توفر الاستضافة قيمة `PORT` تلقائياً في معظم المنصات.

| إعداد الاستضافة | القيمة |
|---|---|
| Runtime | Node.js 22 أو أحدث |
| Build command | `pnpm build` |
| Start command | `pnpm start` |
| Health check | `/` |
| Node process | عملية واحدة طويلة التشغيل |
| HTTPS | مطلوب قبل تشغيل OAuth أو Checkout الحقيقي |

## 2. أنشئ قاعدة بيانات MySQL

من لوحة مزود قاعدة البيانات أنشئ قاعدة MySQL 8، مستخدماً منفصلاً، وكلمة مرور طويلة. إن كنت تستخدم خادماً تملكه، استخدم المثال التالي وعدّل الأسماء والقيمة السرية قبل التشغيل:

```sql
CREATE DATABASE zon_store CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
CREATE USER 'zon_app'@'%' IDENTIFIED BY 'استبدلها_بكلمة_مرور_طويلة';
GRANT ALL PRIVILEGES ON zon_store.* TO 'zon_app'@'%';
FLUSH PRIVILEGES;
```

بعد ذلك جهّز اتصالاً مشفراً إن كان المزود يطلب TLS/SSL. صيغة الاتصال الأساسية هي:

```text
mysql://zon_app:URL_ENCODED_PASSWORD@DB_HOST:3306/zon_store
```

ضع الرابط في متغير `DATABASE_URL` داخل أسرار الاستضافة، وليس في ملف JavaScript أو في Git. الجداول التي ينشئها المشروع هي `users` للمصادقة و`order_tracking` لتتبع حالات الطلبات.

## 3. شغّل ترحيلات قاعدة البيانات

بعد أن تضبط `DATABASE_URL` في الطرفية أو أسرار الاستضافة، نفّذ:

```bash
pnpm db:push
```

يستعمل المشروع Drizzle مع MySQL. ينشئ الأمر migration من `drizzle/schema.ts` ثم يطبقه. لا تشغّل هذا الأمر مع قاعدة إنتاج تحتوي تغييرات يدوية لم تراجعها؛ خذ نسخة احتياطية أولاً.

## 4. اضبط المتغيرات السرية

أضف القيم التالية في لوحة **Environment Variables / Secrets** لدى مزودك:

| المتغير | مثال أو مصدره | ملاحظات |
|---|---|---|
| `NODE_ENV` | `production` | وضع التشغيل. |
| `DATABASE_URL` | رابط MySQL/TiDB | مطلوب للمستخدمين وتتبع الطلبات. |
| `JWT_SECRET` | قيمة عشوائية طويلة | لا تعيد استخدام كلمة مرور. |
| `SHOPIFY_STORE_DOMAIN` | `your-store.myshopify.com` | بلا `https://`. |
| `SHOPIFY_STOREFRONT_API_ACCESS_TOKEN` | Storefront API token | يبقى على الخادم فقط. |
| `VITE_APP_ID` | إعداد OAuth | يطابق إعداد تطبيق المصادقة. |
| `OAUTH_SERVER_URL` | عنوان خادم OAuth | يستخدمه backend. |
| `VITE_OAUTH_PORTAL_URL` | بوابة OAuth | يستخدمه المتصفح لبدء تسجيل الدخول. |
| `OWNER_OPEN_ID` | معرّف مالك المشروع | يمنح دور الأدمن عند تسجيل الدخول. |

لا تضف رموز Shopify الإدارية إلى الواجهة أو إلى `VITE_*`. رمز Storefront يكفي لعرض المنتجات والسلة وCheckout؛ تبقى إنشاءات الخصم الفعلية في Shopify Admin.

## 5. أعد ضبط OAuth وShopify بعد تغيير النطاق

بعد ظهور النطاق الخارجي النهائي، أضف عنوان callback التالي ضمن إعدادات OAuth الخاصة بك:

```text
https://YOUR_DOMAIN/api/oauth/callback
```

وتأكد أن المتجر يعمل تحت HTTPS وأن Shopify يسمح بالوصول إلى Checkout من هذا النطاق. اختبر بعد النشر الصفحة الرئيسية والمتجر وسلة التسوق وفتح Checkout. لا تدخل بيانات دفع حقيقية أثناء الاختبار.

## 6. قائمة فحص الإطلاق

| الفحص | النتيجة المطلوبة |
|---|---|
| `/shop` | منتجات Shopify وصورها وأسعارها تظهر. |
| صفحة المنتج | يمكن اختيار variant وإضافته إلى السلة. |
| السلة | الكمية والحذف يعملان. |
| Checkout | يفتح رابط Shopify الآمن. |
| كود خصم | يُدخل في Checkout وتظهر قيمة الخصم في الإجمالي. |
| `/track-order` | يبحث بالرقم والبريد فقط. |
| OAuth | تسجيل دخول الأدمن يعود إلى نطاقك الجديد. |
| النسخ الاحتياطي | قاعدة البيانات لها نسخة احتياطية مجدولة. |

## ملاحظة وضع الاختبار

صفحتا `/admin/orders` و`/admin/discounts` تعملان حالياً كواجهات اختبار مفتوحة. نموذج الطلبات لا يحفظ شيئاً، ولوحة الخصومات تحفظ **مسودات محلية في المتصفح** وتفتح Shopify لإنشاء الكود الحقيقي. قبل فتح المتجر للجمهور، أعد تقييد أي صفحة إدارة بحساب أدمن وخطط لصلاحيات حقيقية.

