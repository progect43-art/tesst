# تشغيل متجر ZON على الإنترنت

## الخيار الموصى به: استضافة Manus

المشروع يعمل حاليًا ضمن استضافة Manus المدمجة، وهي الأنسب لهذه النسخة لأنها تدعم React وExpress وtRPC وقاعدة البيانات وتكامل Shopify من دون نقل الإعدادات يدويًا. بعد حفظ checkpoint ونشره، استخدم النطاق المنشور من لوحة المشروع. اربط نطاقًا مخصصًا من Settings → Domains إذا كان لديك دومين للبراند.

## استضافة مجانية للواجهة فقط

يمكن رفع نسخة واجهة ثابتة إلى GitHub Pages أو Cloudflare Pages، لكن هذا الخيار لا يناسب ملفات المتجر الحالية وحدها؛ فالسلة وShopify وتتبّع الطلبات تحتاج إلى خادم وبيئة متغيرات وقاعدة بيانات أو خدمة تجارة إلكترونية مستقلة. يصلح الخيار المجاني فقط إذا حوّلت المشروع إلى واجهة ثابتة تعتمد على رابط Shopify Checkout خارجي.

## استضافة مدفوعة للتطبيق الكامل

يمكن استخدام Vercel أو Render أو Railway أو خادم VPS، بشرط تشغيل أمر البناء `pnpm build` ثم تشغيل التطبيق بأمر `pnpm start`، وتوفير Node.js، وقاعدة MySQL/TiDB، ومتغيرات البيئة، ونطاق HTTPS. يجب أيضًا ضبط مسارات `/api/trpc` والاتصال بواجهة Shopify Storefront API. قد تحتاج هذه الخيارات إلى إعدادات إضافية مقارنة باستضافة Manus.

| ما تحتاجه | سبب الحاجة |
|---|---|
| Node.js runtime | تشغيل خادم Express وملف الإنتاج الناتج من البناء |
| قاعدة MySQL/TiDB | المستخدمون وتتبع الطلبات المحلي |
| متغيرات Shopify | قراءة المنتجات وإنشاء السلة وتحويل العميل إلى Checkout |
| HTTPS | حماية الجلسات وعمليات الدفع والطلبات |
| نطاق مخصص | عنوان احترافي للبراند |
| نسخ احتياطية | حماية بيانات الطلبات والإعدادات |

## خطوات الرفع العامة

ثبّت الاعتمادات، شغّل `pnpm check` و`pnpm test`، ثم نفّذ `pnpm build`. على الاستضافة اضبط المتغيرات الموجودة في `.env.example` أو إعدادات المشروع، ثم شغّل `pnpm start`. بعد التشغيل اختبر الصفحة الرئيسية `/`، المتجر `/shop`، صفحة السلة، وتتبّع الطلبات `/track-order`.

لا تضع مفاتيح Shopify أو قاعدة البيانات داخل ملفات JavaScript أو GitHub. احفظها في Secrets/Environment Variables لدى مزود الاستضافة، وتأكد من أن نطاق الموقع مضاف إلى إعدادات Shopify قبل استقبال الطلبات.
